# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
dist/
*.egg-info/

# Data + DB (the 72 GB DuckDB file and its logs are the expensive artifact; rebuild via `kalshi-flb backfill`)
# Anchored to the repo root so it does NOT match web/data/ (the committed dashboard data).
/data/
*.duckdb
*.duckdb.wal

# Local editor / agent state — never commit
.claude/settings.local.json
.remember/
.DS_Store
.playwright-mcp/
